# WorkTime Sync

**WorkTime Sync** — веб-система для актуализации рабочего времени сотрудников. Проект помогает видеть реальные рабочие графики, конфликты с календарём, перегрузки, расхождения с HR-данными, статусы подтверждения графиков и задачи, которые нужно выполнить для устранения проблем.

Проект разработан для кейса №3 хакатона **«Весенний код»**: **«Система по актуализации рабочего времени сотрудников»**.

---

## 1. Суть проекта

В компаниях рабочее время сотрудников часто хранится в разных местах: HR-системе, календарях, таск-трекерах, табелях и личных договорённостях. Эти данные быстро устаревают: сотрудники меняют формат работы, переходят на удалёнку или гибрид, работают из другого часового пояса, уходят в отпуск или получают встречи вне рабочего времени.

**WorkTime Sync** решает эту проблему как аналитический и workflow-контур:

1. собирает или имитирует данные о сотрудниках, графиках, встречах, HR-профилях и отсутствиях;
2. сравнивает заявленный график с фактической активностью;
3. рассчитывает актуальность графика, загрузку, конфликты и риск;
4. показывает проблемные зоны руководителям и HR;
5. позволяет создавать задачи сотрудникам;
6. позволяет сотрудникам подтверждать график или отвечать на задачи;
7. помогает снижать конфликты, переработки и ручную координацию.

Главная логика:

```text
аналитика → задача → подтверждение сотрудником → актуализация графика
```

---

## 2. Текущее состояние проекта

### Backend V2.9

Backend обновлён до версии **2.9.0**.

| Возможность | Статус |
|---|---|
| FastAPI backend | реализовано |
| Чтение synthetic CSV/JSON | реализовано |
| Demo-login | реализовано |
| Demo-token `Authorization: Bearer demo-...` | реализовано |
| Роли пользователей | реализовано |
| Scope-доступ | реализовано |
| Фильтрация данных по роли пользователя | реализовано |
| Личный кабинет сотрудника на уровне API | реализовано |
| Задачи `tasks.json` | реализовано |
| API задач | реализовано |
| Подтверждение графика | реализовано |
| Подтверждения в `schedule_confirmations.json` | реализовано |
| Задачи по встречам | реализовано на уровне данных и API |
| `related_event_id` для задач по встречам | реализовано |
| Проверка связи задачи и события | реализовано |
| Риск с весами по отделам | реализовано |
| Company analytics | реализовано |
| HR dashboard API | реализовано |
| Data-quality для tasks/confirmations | реализовано, можно расширять дальше |
| Production JWT | не реализовано, используется demo-token |

### Frontend

Frontend реализован на **React + Vite** и подключён к backend API.

| Возможность | Статус |
|---|---|
| React + Vite | реализовано |
| Экран входа | реализовано |
| Demo-login через backend | реализовано |
| Передача пользователя в API | реализовано |
| Role/scope-загрузка данных | реализовано |
| Дашборд отдела | реализовано |
| Список сотрудников | реализовано |
| Карточка сотрудника | реализовано |
| Конфликты | реализовано |
| Доступность | реализовано |
| Рекомендации | реализовано |
| Executive dashboard UI | реализовано |
| HR dashboard UI | реализовано |
| Employee cabinet UI | реализовано |
| UI задач | реализовано |
| Подтверждение графика из UI | реализовано |
| Создание задач из UI | реализовано |
| Управление задачами по встречам из UI | реализовано частично: ответ сотрудника, применение решения и перенос через backend |

---

## 3. Ролевая модель

| Роль | Scope | Что видит | Что может делать |
|---|---|---|---|
| `executive` | `all` | всю компанию, все отделы, всех сотрудников | смотреть общую аналитику, контролировать отделы, создавать задачи любому сотруднику |
| `hr` | `all` | всех сотрудников, HR-расхождения, подтверждения | создавать HR-задачи, проверять графики и HR-данные |
| `department_manager` | `department` | только сотрудников своего отдела | создавать задачи сотрудникам своего отдела, контролировать графики, встречи и нагрузку |
| `employee` | `self` | только свой профиль и свои задачи | подтверждать график, подтверждать/отклонять задачи, оставлять комментарии |

---

## 4. Demo-пользователи

В проекте есть demo-пользователи для всех основных ролей.

### Руководители и HR

| Логин | Пароль | Роль | Доступ |
|---|---|---|---|
| `executive_demo` | `test0` | полный руководитель | вся компания |
| `hr_demo` | `testhr` | HR | все сотрудники |
| `zarix` | `i9VUibm6` | руководитель отдела | Core Platform |
| `lixxxa` | `test1` | руководитель отдела | Product UI |
| `baftype` | `test2` | руководитель отдела | People Ops |
| `ssdshkaaa` | `test3` | руководитель отдела | Delivery |
| `agentemy` | `test4` | руководитель отдела | Quality |

### Сотрудники

Для каждого сотрудника есть отдельный аккаунт:

```text
employee1 / emp1
employee2 / emp2
...
employee25 / emp25
```

Дополнительный удобный demo-login:

| Логин | Пароль | Роль | Сотрудник |
|---|---|---|---|
| `gleb_employee` | `emp5` | сотрудник | Глеб Соколов |

---

## 5. Данные проекта

Основной источник данных:

```text
data/synthetic/
```

| Dataset | Назначение |
|---|---|
| `employees.csv` | сотрудники, отделы, рабочие графики |
| `events.csv` | календарные события и встречи |
| `hr_profiles.csv` | HR-данные для сверки |
| `absences.csv` | отпуска, больничные, day off |
| `users.json` | demo-пользователи и роли |
| `tasks.json` | задачи от руководителей/HR сотрудникам |
| `schedule_confirmations.json` | статусы подтверждения графиков |

Текущая выборка:

| Показатель | Значение |
|---|---:|
| сотрудников | 25 |
| отделов | 5 |
| календарных событий | 60 |
| HR-профилей | 25 |
| отсутствий | 3 |
| demo-пользователей | 33 |
| задач | 24 |
| подтверждений графика | 25 |

---

## 6. Задачи в системе

Сейчас реализован общий механизм задач: руководитель, HR или executive может создать задачу сотруднику, а сотрудник может подтвердить, отклонить или запросить перенос/изменение.

| Тип | Что значит |
|---|---|
| `confirm_schedule` | подтвердить рабочий график |
| `review_hr_profile` | проверить HR-профиль |
| `review_load` | проверить нагрузку |
| `update_timezone` | проверить или обновить часовой пояс |
| `meeting_confirmation` | подтвердить участие во встрече |
| `reschedule_meeting` | согласовать перенос встречи |
| `meeting_outside_work_approval` | согласовать встречу вне рабочего времени |

Статусы задач:

| Статус | Что значит |
|---|---|
| `pending` | задача ожидает действия |
| `confirmed` | сотрудник подтвердил задачу |
| `rejected` | сотрудник отклонил задачу |
| `done` | задача закрыта |
| `expired` | задача просрочена |
| `reschedule_requested` | сотрудник запросил перенос |

Важно: сама встреча хранится в `events.csv`, а задача — это действие вокруг этой встречи.

```text
events.csv  → сама встреча
tasks.json  → подтвердить / перенести / согласовать эту встречу
```

Для задач по встречам используется поле `related_event_id`, которое связывает задачу с конкретной встречей.

---

## 7. Формулы и метрики

Актуальность графика остаётся единой для всех отделов:

```text
A = max(0, 1 - days_since_update / 90)
```

Риск рассчитывается по общей структуре, но с весами отдела:

```text
R = wA * (1 - A)
  + wC * C
  + wL * L
  + wT * timezone_mismatch
  + wH * hr_mismatch
```

| Отдел | `wA` | `wC` | `wL` | `wT` | `wH` |
|---|---:|---:|---:|---:|---:|
| Core Platform | 0.25 | 0.25 | 0.30 | 0.10 | 0.10 |
| Product UI | 0.25 | 0.30 | 0.25 | 0.10 | 0.10 |
| People Ops | 0.30 | 0.15 | 0.20 | 0.10 | 0.25 |
| Delivery | 0.20 | 0.30 | 0.30 | 0.10 | 0.10 |
| Quality | 0.20 | 0.25 | 0.35 | 0.10 | 0.10 |

---

## 8. Backend API

| Endpoint | Назначение |
|---|---|
| `POST /auth/login` | demo-вход |
| `GET /health` | проверка backend |
| `GET /health/data` | проверка активных данных |
| `GET /api/worktime/overview` | основной endpoint frontend |
| `GET /employees/me` | личный кабинет сотрудника на уровне API |
| `GET /employees/{employee_id}/risk-explanation` | объяснение риска |
| `PATCH /employees/{employee_id}/confirm-schedule` | подтверждение графика |
| `GET /analytics/company` | аналитика всей компании |
| `GET /analytics/hr-dashboard` | HR-dashboard API |
| `GET /tasks` | список задач |
| `GET /tasks/my` | задачи сотрудника |
| `POST /tasks` | создать задачу |
| `GET /tasks/{task_id}` | открыть задачу |
| `PATCH /tasks/{task_id}/status` | изменить статус задачи |

Примеры через `user_id`:

```text
GET /api/worktime/overview?user_id=u0
GET /api/worktime/overview?user_id=u1
GET /api/worktime/overview?user_id=emp5
GET /tasks?user_id=u1
GET /tasks/my?user_id=emp5
GET /analytics/company?user_id=u0
GET /analytics/hr-dashboard?user_id=u_hr
```

Примеры через demo-token:

```http
GET /api/worktime/overview
Authorization: Bearer demo-zarix
```

```http
GET /tasks
Authorization: Bearer demo-agentemy
```

```http
GET /employees/me
Authorization: Bearer demo-employee5
```

---

## 9. Что нужно сделать дальше

### Frontend

1. Сделать role-based routing.
2. Сделать ExecutiveDashboard.
3. Сделать HRDashboard.
4. Сделать EmployeeCabinet.
5. Добавить UI задач.
6. Добавить создание задач руководителем/HR из интерфейса.
7. Добавить подтверждение/отклонение задач сотрудником из интерфейса.
8. Добавить подтверждение графика из интерфейса.
9. Добавить отображение задач по встречам.
10. Добавить отображение `related_event` в карточке задачи.

### Backend

1. Добавить endpoint применения решения по задаче встречи.
2. Добавить изменение события при подтверждённом переносе.
3. Добавить `save_events()` для обновления `events.csv/json`.
4. Добавить автоматическую генерацию задач по проблемным встречам.
5. Добавить тесты на demo-token authorization.
6. Добавить тесты задач по встречам.
7. Добавить тесты синхронизации `confirm_schedule` и `schedule_confirmations`.

### Данные

1. Добавить больше реалистичных отсутствий.
2. Добавить историю изменений графиков.
3. Добавить больше HR-расхождений.
4. Добавить сценарии просроченных задач.
5. Добавить сценарии конфликтов между отсутствиями и встречами.

---

## 10. Запуск

### Backend: PowerShell / Windows

```powershell
cd backend
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

### Backend: Git Bash / Windows

```bash
cd backend
py -3.12 -m venv .venv
source .venv/Scripts/activate
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

### Тесты backend

```bash
cd backend
python -m pytest
---

## 11. Демо-сценарий текущей версии

1. Запустить backend.
2. Открыть Swagger: `/docs`.
3. Проверить `/health`.
4. Проверить `/health/data`.
5. Войти через `/auth/login`.
6. Скопировать `token` из ответа.
7. Проверить `/api/worktime/overview` с `Authorization: Bearer demo-zarix`.
8. Проверить `/api/worktime/overview` с `Authorization: Bearer demo-executive_demo`.
9. Проверить `/employees/me` с `Authorization: Bearer demo-employee5`.
10. Проверить `/tasks` с `Authorization: Bearer demo-zarix`.
11. Проверить `/tasks/my` с `Authorization: Bearer demo-employee5`.
12. Проверить `/analytics/company` с `Authorization: Bearer demo-executive_demo`.
13. Проверить `/analytics/hr-dashboard` с `Authorization: Bearer demo-hr_demo`.

Для ручной отладки также можно использовать старый формат:

```text
GET /api/worktime/overview?user_id=u1
GET /tasks?user_id=u1
GET /employees/me?user_id=emp5
```

---

## 12. Ограничения MVP

- нет production JWT-авторизации, используется demo-token;
- пароли demo-пользователей лежат в JSON;
- нет базы данных;
- задачи и подтверждения хранятся в JSON;
- нет реальных интеграций с календарём, HRM и таск-трекером;
- нет реальных push/email/Telegram-уведомлений;
- production JWT и реальные интеграции пока не реализованы;
- история изменений графиков пока минимальная.

---

## 13. Краткий вывод

**WorkTime Sync** развивается от обычного дашборда к системе актуализации рабочего времени через роли, задачи и подтверждения. Backend V2.9 уже поддерживает role/scope-доступ, demo-token, задачи, подтверждения графиков и задачи по встречам. Следующий главный шаг — вывести эти backend-возможности во frontend и добавить полноценное применение решений по задачам к данным встреч и графиков.
---

## Обновление V2.9.0

V2.9.0 закрывает проблему `Not Found` и `405` при работе форм. Frontend теперь использует единый префикс `/api`, а backend дублирует основные маршруты в `/api/...`. Это важно для продакшена, где Nginx обычно проксирует на backend только `/api/`, а остальные пути отдаёт React-приложению.

Что добавлено:

- `/api/auth/login`;
- `/api/tasks`, `/api/tasks/my`, `/api/tasks/meta`;
- `/api/tasks/{task_id}/status`;
- `/api/tasks/{task_id}/apply`;
- `/api/tasks/generate-from-conflicts`;
- `/api/employees/me`;
- `/api/employees/{employee_id}/confirm-schedule`;
- `/api/analytics/company`;
- `/api/analytics/hr-dashboard`;
- `/api/analytics/availability`.

Во frontend добавлены фильтры задач по статусу, отделу исполнителя и тому, кто создал задачу. На карточках задач теперь отображаются теги без подписей `Создал:` и `Исполнитель:`.
