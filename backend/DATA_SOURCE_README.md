# Data source patch

Backend source of truth for demo data:

```text
data/synthetic/
```

Expected backend-ready files:

```text
employees.json or employees.csv
events.json or events.csv
hr_profiles.json or hr_profiles.csv
absences.json or absences.csv
```

`backend/data/` is kept only as a temporary fallback while the project migrates.
